/** @type {import('next').NextConfig} */
const nextConfig = {
   images: {
      domains: ['i.scdn.co', 'media.discordapp.net', 'cdn.discordapp.com'], // İlgili resim kaynağı URL'sini ekleyin.
   },
};

export default nextConfig;
